This file is a placeholder. The real gradle-wrapper.jar must be generated by running:
  gradle wrapper
...inside the project directory, or by cloning a real Android project and copying the jar.

Alternatively, GitHub Actions will handle this automatically when you push to the repo.
